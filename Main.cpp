#include "bits/stdc++.h"

using namespace std;

int main()
{
    string name = "Hardik";
    int n = name.length();
    for (auto &item : name)
    {
        cout << item << " ";
    }

    return 0;
}