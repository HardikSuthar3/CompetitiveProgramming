#include "bits/stdc++.h"
using namespace std;

int main()
{
    string name = "Hardik";
    int n = name.length();
    for (size_t i = 0; i < n; i++)
    {
        cout << name[i] << " ";
    }

    return 0;
}